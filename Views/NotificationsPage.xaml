<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="YessGoFront.Views.NotificationsPage"
    x:Name="NotificationsPageRoot"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:YessGoFront.Views.Controls"
    Shell.NavBarIsVisible="False"
    BackgroundColor="#F4F6F8"
    x:DataType="viewModel:NotificationsViewModel"
    xmlns:viewModel="clr-namespace:YessGoFront.ViewModels">

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Grid Grid.Row="0" Padding="16,12" BackgroundColor="#007A51">
            <Label Text="Уведомления" 
                   TextColor="White" 
                   FontSize="20" 
                   FontAttributes="Bold"
                   VerticalTextAlignment="Center"/>
        </Grid>

        <!-- Notifications list -->
        <RefreshView Grid.Row="1" Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
            <ScrollView>
                <VerticalStackLayout Padding="16" Spacing="12">
                    <!-- Loading indicator -->
                    <ActivityIndicator IsRunning="{Binding IsBusy}" 
                                     IsVisible="{Binding IsBusy}"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center"
                                     HeightRequest="40"/>

                    <!-- Error message -->
                    <Frame BackgroundColor="#FEE2E2" 
                           CornerRadius="8" 
                           Padding="12"
                           IsVisible="{Binding HasError}"
                           HasShadow="False">
                        <StackLayout Spacing="4">
                            <Label Text="Ошибка загрузки уведомлений" 
                                   TextColor="#991B1B" 
                                   FontAttributes="Bold"/>
                            <Label Text="{Binding ErrorMessage}" 
                                   TextColor="#7F1D1D" 
                                   FontSize="12"/>
                        </StackLayout>
                    </Frame>

                    <!-- Empty state -->
                    <StackLayout IsVisible="{Binding Notifications.Count, Converter={StaticResource InverseBoolConverter}}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="12">
                        <Label Text="🔔" 
                               FontSize="48" 
                               HorizontalOptions="Center"/>
                        <Label Text="У вас пока нет уведомлений" 
                               TextColor="#6B7280" 
                               HorizontalOptions="Center"/>
                    </StackLayout>

                    <!-- Notifications collection -->
                    <CollectionView ItemsSource="{Binding Notifications}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame CornerRadius="14" 
                                       BackgroundColor="{Binding IsRead, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#FFFFFF:#F9FAFB'}" 
                                       HasShadow="True" 
                                       Padding="12"
                                       Margin="0,0,0,8">
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.MarkAsReadCommand}" 
                                                            CommandParameter="{Binding .}"/>
                                    </Frame.GestureRecognizers>
                                    
                                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                                        <!-- Notification Icon -->
                                        <Frame Grid.Column="0" 
                                               BackgroundColor="{Binding Priority, Converter={StaticResource PriorityToColorConverter}}" 
                                               CornerRadius="10" 
                                               Padding="10"
                                               HeightRequest="44"
                                               WidthRequest="44"
                                               VerticalOptions="Center">
                                            <Label Text="{Binding Icon}" 
                                                   FontSize="20"
                                                   HorizontalTextAlignment="Center"
                                                   VerticalTextAlignment="Center"/>
                                        </Frame>
                                        
                                        <!-- Notification Content -->
                                        <StackLayout Grid.Column="1" Spacing="4">
                                            <Label Text="{Binding Title}" 
                                                   FontSize="14" 
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding IsRead, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#111827:#6B7280'}"/>
                                            <Label Text="{Binding Message}" 
                                                   FontSize="13" 
                                                   TextColor="{Binding IsRead, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#4B5563:#9CA3AF'}"
                                                   MaxLines="2" 
                                                   LineBreakMode="TailTruncation"/>
                                            <Label Text="{Binding CreatedAt, StringFormat='{0:dd MMM yyyy, HH:mm}'}" 
                                                   FontSize="11" 
                                                   TextColor="#9CA3AF"/>
                                        </StackLayout>
                                        
                                        <!-- Unread indicator -->
                                        <BoxView Grid.Column="2" 
                                                 WidthRequest="8" 
                                                 HeightRequest="8" 
                                                 CornerRadius="4"
                                                 Color="#3B82F6"
                                                 VerticalOptions="Center"
                                                 IsVisible="{Binding IsRead, Converter={StaticResource InverseBoolConverter}}"/>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </ScrollView>
        </RefreshView>
        <controls:BottomNavBar x:Name="BottomBar" Grid.Row="2" />
    </Grid>
</ContentPage>
