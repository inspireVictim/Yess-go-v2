# Конфигурация API - Централизованные настройки

## 📍 Изменение IP-адреса сервера

Все HTTP-запросы приложения идут на один централизованно настроенный сервер.

### Как изменить IP-адрес сервера

Откройте файл **`Config/ApiConfiguration.cs`** и измените константу:

```csharp
public const string BASE_URL = "http://5.59.232.211:8000";
```

На ваш адрес сервера, например:
```csharp
public const string BASE_URL = "http://192.168.1.100:8000";
// или
public const string BASE_URL = "https://api.example.com";
```

### Переопределение через переменную окружения

Вы также можете переопределить адрес через переменную окружения `API_BASE_URL`:

**Android/iOS:**
```bash
export API_BASE_URL="http://your-server-ip:8000"
```

**Windows:**
```powershell
$env:API_BASE_URL="http://your-server-ip:8000"
```

### Где используется

После изменения `ApiConfiguration.BASE_URL` все HTTP-запросы автоматически будут идти на новый адрес:

- ✅ `GET /health`
- ✅ `GET /users`
- ✅ `POST /login`
- ✅ Все остальные API endpoints

### Текущая конфигурация

- **Базовый URL:** `http://5.59.232.211:8000`
- **Версия API:** `v1`
- **Полный URL:** `http://5.59.232.211:8000/api/v1`

### Важные замечания

1. ✅ **Не используйте магические строки** в коде - все URL берутся из `ApiConfiguration`
2. ✅ Все `HttpClient` настроены через `MauiProgram.cs` и используют единый `BaseAddress`
3. ✅ Кроссплатформенность: один IP-адрес работает на Android, iOS и Windows

### Структура файлов

- `Config/ApiConfiguration.cs` - централизованная конфигурация (ИЗМЕНИТЕ ЗДЕСЬ)
- `Config/AppSettings.cs` - настройки приложения (использует ApiConfiguration)
- `MauiProgram.cs` - регистрация HttpClient с BaseAddress
- `Infrastructure/Http/ApiClient.cs` - базовый класс для API клиентов

---

**Последнее обновление:** 2024

