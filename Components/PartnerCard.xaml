<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YessGoFront.Components.PartnerCard">
    <Frame Padding="15" Margin="5" BackgroundColor="White" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Text="{Binding Name}" FontSize="16" FontAttributes="Bold" />
            <Label Grid.Row="1" Text="{Binding Cashback, StringFormat='Кэшбэк: {0}%'}" FontSize="14" TextColor="Green" />
        </Grid>
    </Frame>
</ContentView>
